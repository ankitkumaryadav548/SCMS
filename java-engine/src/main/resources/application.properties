# Server Port config (reads PORT from cloud host e.g. Railway/Render)
server.port=${PORT:8081}

# Server path prefix
server.servlet.context-path=/api/v1

# Logging details
logging.level.org.springframework.web=INFO
logging.level.com.smartcity.engine=DEBUG
